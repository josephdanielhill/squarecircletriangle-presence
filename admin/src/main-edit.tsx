import React from 'react';
import ReactDOM from 'react-dom/client';
import { GuestEditApp } from './GuestEditApp';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <GuestEditApp />
  </React.StrictMode>
);
